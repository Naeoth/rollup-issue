{
  "name": "issue",
  "scripts": {
    "build": "rm -rf dist/* && rollup --config",
    "start": "NODE_ENV=production node dist/index.js",
    "check-build": "echo Error: `grep '\\./' dist/index.js | wc -l`"
  },
  "devDependencies": {
    "@babel/core": "^7.11.6",
    "@rollup/plugin-node-resolve": "^9.0.0"
  },
  "dependencies": {
    "moment": "^2.29.1",
    "rollup": "^2.28.2"
  }
}
